# tjj 组代码学习导读

> 本目录是从同学组论文《无线电干扰源的快速自动定位与清除——基于集员估计的交会定位、稳健选址与方向完备在线清除》**PDF 附录中还原出来的源代码**，并逐文件加了通俗注释。
> 还原方法、可信度、以及"哪些值得学、哪些我们实测后不采纳"都写在下面。

---

## 一、这批代码是怎么来的（可信度说明）

论文 PDF 的附录 A~D 里带了 **7 个源文件、约 7700 行**代码。PDF 抽取有个大坑：
**按空格数还原缩进会抖动**（同一层级实测 4~7 个空格，层级间隔约 5，在 7 附近两义），
直接导致 `unexpected indent` / `expected an indented block`，而且无法机械收敛。

最终改用**文本块精确坐标**还原：

```
缩进层级 = round( (该行代码块横坐标 − 全局代码列左边界 x0) / 一级缩进宽度 )
实测标定：x0 = −23.19 pt，一级缩进 = 12.15 pt
```

坐标本身没有抖动，层级判定就精确了。结果：

| 文件 | 行数 | 对应论文 | 语法校验 |
|---|---|---|---|
| `q1.py` | 342 | 附录 A 问题一：交会定位 | ✅ 通过 |
| `q2.py` | 1823 | 附录 B.1 问题二主程序 | ✅ 通过 |
| `q2_decision.py` | 330 | 附录 B.2 决策模块 | ✅ 通过 |
| `q2_evaluation.py` | 79 | 附录 B.3 评价模块 | ✅ 通过 |
| `q2_evidence.py` | 489 | 附录 B.4 结果输出 | ✅ 通过 |
| `q3.py` | 1852 | 附录 C 问题三 | ✅ 通过 |
| `q4.py` | 3040 | 附录 D 问题四 | ✅ 通过 |

全部通过 `ast.parse` 语法校验。**注意两点**：
1. 原代码里的空行在 PDF 中只留行号，无法还原，已被丢弃（不影响逻辑）；
2. 所有新增注释都以 `# 【学习注释】` 开头，与他人的原始注释区分开，逻辑一行未改。

> 另外提醒：他们把队号写成了 `ROBOT_ID = "********"` —— **这是提交前的标准做法**（我们后来也把队号清成了 `<参赛队号>`）。

---

## 二、成绩对比（都是正式测试）

| 指标 | tjj 组 | 我们 | 差距 |
|---|---|---|---|
| 问题三 s/源 | **234.49**（228.15 / 236.49 / 238.82） | 311.51（307.77 / 313.49 / 313.26） | 他们快 **24.7%** |
| 问题三 行程/局 | ~13.30 km | ~16.78 km | 他们少 **20.7%** |
| 问题四 s/源 | 474.55（354.23 / 572.39 / 497.01） | 544.56（540.00 / 513.65 / 580.02） | 他们快 **12.9%** |
| 问题四 行程/局 | ~23.27 km | ~24.43 km | 他们少 4.7% |
| 清除比例 | 两次 3 组均 100% | 两次 3 组均 100% | 持平 |

他们的完备性自证写得很漂亮：**「清除数 + 认证空频道数 = 20（频道总数）」**，
不需要真值就能说明"本局所有频道都已定性"。

---

## 三、设计范式对比

| 环节 | tjj 组 | 我们 | 评价 |
|---|---|---|---|
| 误差范式 | 集员估计（误差与地点绑定，不可平均） | 同 | 一致，都对 |
| 问题一 | 角域→半平面，Sutherland–Hodgman | 同 | 一致 |
| 问题三骨架 | **中心 + 半径 1010 m 七等角 = 8 点** | 中心 + 半径 1000 m 14 点（其中 8 个全频道扫描点） | 他们半径取 1010 更优（见下） |
| 最坏覆盖 | **992.057 m**（余量 7.94 m） | 998.25 m（余量 1.75 m） | **他们胜** |
| 复测点 | 无 | 7 个"几何复测点" | **我们胜**（实测见下） |
| 任务排序 | 强制任务集用 **bitmask 精确 DP**（≤12 点） | 最便宜插入 + 2-opt/Or-opt | 他们更彻底 |
| 决策门控 | 精确解需省 ≥ **250 m**（regret）才覆盖原决策 | 无（直接采用） | 他们更稳 |
| 清除时机 | **三类"试探清除"**，几何够好就试清（失败仅 3 s） | 最便宜插入（阈值 2500 m）+ 机会清除（400 m） | **他们更激进 → 行程更短** |
| 每源测量次数 | ≈ **3.3 次**（N=15） | ≈ **6.4 次**（N=16） | **差距主要在这里** |
| 问题四骨架 | **中心 1 + 990 m 内环 7 + 外接正十四边形外环 14 = 22 点** | 中心 1 + 1000 m 内环 9 + 1875 m 外环 12 = 22 点 | 都是 22 点，构型不同 |
| 方向完备证书 | 2 m 网格离线证书（距离余量 1.586 m、凸包余量 7.187 m） | 11280 采样点角隙证书（最大角隙 177.3768°） | 他们网格更密 |
| 收尾优化 | **SAFE 层**（只吃严格支配收益 + 锚定下一动作） | 27 m 网格保证性兜底 | 两者互补 |

---

## 四、我的实测结论：哪些值得学（已在我们框架里跑过）

对照实验脚本：`scripts/exp_tjj_ideas.py`

### ✅ 采纳：骨架半径 1000 → 1010 m

最坏覆盖距离 d_max = √(1800² + a² − 2·1800·a·cos(π/7))：

| 环半径 a | 最坏覆盖 | 余量 |
|---|---|---|
| 1000 m | 998.255 m | 1.75 m |
| **1010 m** | **992.057 m** | **7.94 m** |
| 1015 m | 988.982 m | 11.02 m |

**时间几乎不变**（257.0 → 256.8 s/源），但**余量从 1.75 m 提到 7.94 m**。
这是"零成本换稳健性"的改进，值得直接采纳（我们原来的 1.75 m 余量对采样误差过于敏感）。

### ❌ 不采纳：去掉"几何复测点"（他们的骨架只有 8 个扫描点）

| probe_keep | s/源 | 行程 km | 测量 |
|---|---|---|---|
| **0（= 他们的设计）** | **272.5** | 17.53 | 125.0 |
| 2 | 265.2 | 16.70 | 132.8 |
| **4（我们的默认）** | **257.0** | **16.03** | 133.9 |
| 5 | 255.1 | 15.87 | 134.7 |

**照他们的设计删掉复测点反而慢了 15.5 s/源** —— 我们那 7 个复测点虽然多花约 9 次测量，
但把收尾行程压下去了，净收益为正。**这一维度我们更优，保留。**

### ❌ 不采纳：收紧插入阈值（我们行程偏长不是这个原因）

| insert_limit | s/源 | 行程 km |
|---|---|---|
| **2500（默认）** | **257.0** | **16.03** |
| 800 | 263.9 | 16.49 |
| 400 | 258.9 | 16.07 |
| 200 | 259.8 | 16.11 |
| 100 | 262.2 | 16.31 |

问题四的机会清除阈值同理：400 m 已是最优（收紧到 80 m 反而 465.0 → 469.4）。

### 🔶 待验证（差距的真正所在）：他们的"试探清除"调度

把两边数据拆开看，**差距来自两处**：
1. **每源测量次数**：他们 ≈3.3 次，我们 ≈6.4 次 ⇒ 多约 3 次 ×6 s = **18 s/源**；
2. **行程**：13.30 vs 16.03 km ⇒ 多 2.7 km = 540 s ⇒ **34 s/源**。

两项合计约 **52 s/源**，与实测差距（约 70 s/源）同量级。

他们的做法是把清除当成**被奖励的动作**而不是**要插入的绕行**：
`opportunistic_score` 给"保证能清"320 m、"强 Probe"150 m、"弱 Probe"60 m 的等效奖励，
一律按「移动距离 − 等效奖励」排序；**因为清除失败只耗 3 s，由 p > 3/(ΔT+3) 得门槛仅约 6%，
所以可以大胆顺路试清**——于是源常常在"路过"时就被清掉，不需要额外补测量、也不需要专门绕路。

我们的模型是"把清除点插进剩余骨干路线（阈值 2500 m）"，
更偏"规划"，少了这种"**低成本试探 + 收益排序**"的机制。
**这是下一步最值得尝试的改造方向**（需要改调度层，属于结构性改动）。

---

## 五、还值得借鉴的 3 个写法（工程习惯）

1. **`certify_empty` 认证空频道**：把"未发现"变成"可证明为空"，于是 `清除数 + 空频道数 = 20` 成为**自检证书**。我们目前对"从未被发现的源"是无感知的（`unresolved` 为空）。
2. **regret 门控**：精确解只有在省 ≥250 m 时才覆盖原决策 —— 避免"为几米收益频繁改路线"造成的抖动。我们缺少这一层。
3. **SAFE 层**：只接受"严格更短 且 仍严格保证清除"的局部改动，并**锚定下一动作**，保证"同一前状态 → 同一决策"。这与我们的 27 m 网格兜底互补，可以叠加。

---

## 六、怎么读这批代码（建议顺序）

1. **先读 `q3.py` 顶部的常量表**（文件头注释里我整理了对照表），把参数含义搞清楚；
2. 再读 `build_backbone` + `analytic_cover_radius`：理解 8 点骨架与 992.057 m 的来历；
3. 读 `certify_empty`：理解完备性怎么被"认证"出来；
4. 读 `_mandatory_open_tsp_first` + `apply_mandatory_route_guard`：精确 DP 与它的门控；
5. 最后读 `opportunistic_score` + `choose_action`：调度思想（**最值得学的一段**）；
6. `q4.py` 按 `validate_direction_complete_backbone` → `run_geometry_self_test` →
   `is_strict_guaranteed_clear_action` → `safe_transform_selected_action` 的顺序读。

`q2*.py` 与我们的问题一、二用法不同（他们是稳健优化的完整实现），
如果只关注问题三、四，可以先跳过，需要时再看。

---

## 七、一句话总结

> 他们的优势**不在骨架设计**（我们的复测点更优），而在**把清除当作"低成本试探 + 收益排序"的动作**：
> 每源少测约 3 次、少走约 2.7 km。
> 可以直接拿走的只有一件事：**环半径取 1010 m（余量 1.75 → 7.94 m，零成本）**。
