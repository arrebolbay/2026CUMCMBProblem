"""全局常量：全部取自 2026 CUMCM B 题正文、附件1、附件2。

凡模拟器协议中的默认值均在此集中定义，便于灵敏度分析时统一改动。
"""

# ---------------------------------------------------------------- 目标区域
REGION_RADIUS = 1800.0        # 目标区域半径 (m)，圆心为原点
COORD_LIMIT = 2_000_000.0     # 接口允许的坐标绝对值上限 (m)

# ---------------------------------------------------------------- 物理参数
MOVE_SPEED = 5.0              # 机器狗移动速度 (m/s)，题给
EPS_DEG = 1.0                 # 示向度误差界 ±1°，题给
R_MIN = 1000.0                # 有效接收半径下界 (m)，题给 1000~1500
R_MAX = 1500.0                # 有效接收半径上界 (m)
NEAR_RADIUS = 5.0             # 近距离阈值 (m)，/measure 返回 "near"
CLEAR_RADIUS = 20.0           # 清除半径 (m)，/clear 成功条件

# ---------------------------------------------------------------- 动作耗时
MEASURE_TIME = 5.0            # 检测动作耗时 (s)
SWITCH_TIME = 1.0             # 任意频道切换耗时 (s)
CLEAR_FAIL_TIME = 3.0         # /clear 未发现：仅精确定位耗时 (s)
CLEAR_SUCCESS_TIME = 5.0      # /clear 成功：精确定位3s + 激光清除2s (s)

# ---------------------------------------------------------------- 频道/干扰源
CHANNELS = tuple(range(1, 21))     # 频道 1..20，每频道至多 1 个干扰源
JAMMER_NUM_RANGE = (10, 16)        # 干扰源总数范围（未知，仅演练结束可见）

# ---------------------------------------------------------------- 时间限制
MAX_VIRTUAL_DURATION = 360_000.0   # 虚拟世界限时 (s) = 100 h
MAX_REAL_DURATION = 1200           # /enter 后程序运行上限 (s) = 20 min
WINDOW_DURATION = 1500             # 25 分钟测试窗口 (s)

# ---------------------------------------------------------------- 接口
DEFAULT_BASE_URL = "http://127.0.0.1:2026"
ARENA_ID = "default"
